<!-- Vistas/Pacientes.xaml -->
<UserControl x:Class="ClinicaRodriguez.Vistas.Pacientes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:convertidores="clr-namespace:ClinicaRodriguez.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.Resources>
        <convertidores:FechaNulaConverter x:Key="FechaNulaConverter"/>

        <Style x:Key="EncabezadoGrid" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FF171D67"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,0,0,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Gestión de pacientes"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="#FF171D67"
                   Margin="0,0,0,15"/>

        <TextBox Grid.Row="1"
                 Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}"
                 Width="300"
                 HorizontalAlignment="Left"
                 Padding="10,8"
                 FontSize="14"
                 Margin="0,0,0,15"/>

        <DataGrid x:Name="dgPacientes"
                  Grid.Row="2"
                  ItemsSource="{Binding Pacientes}"
                  SelectedItem="{Binding PacienteSeleccionado, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserResizeColumns="False"
                  CanUserReorderColumns="False"
                  CanUserResizeRows="False"
                  Background="White"
                  BorderBrush="#FFE0E0E0"
                  BorderThickness="1"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#FFE0E0E0"
                  RowHeight="40"
                  ColumnHeaderHeight="45"
                  HorizontalScrollBarVisibility="Auto"
                  SelectionMode="Single"
                  ColumnHeaderStyle="{StaticResource EncabezadoGrid}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="Auto"/>
                <DataGridTextColumn Header="Apellido" Binding="{Binding Apellido}" Width="Auto"/>
                <DataGridTextColumn Header="DNI" Binding="{Binding DNI}" Width="*"/>
                <DataGridTextColumn Header="Correo" Binding="{Binding Correo}" Width="Auto"/>
                <DataGridTextColumn Header="Nacimiento" Binding="{Binding FechaNacimiento, Converter={StaticResource FechaNulaConverter}}" Width="Auto"/>
                <DataGridTextColumn Header="Edad" Binding="{Binding Edad}" Width="Auto"/>
                <DataGridTextColumn Header="Residencia" Binding="{Binding Residencia}" Width="*"/>
                <DataGridTextColumn Header="Ocupación" Binding="{Binding Ocupacion}" Width="Auto"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding TelefonoPrimario}" Width="Auto"/>
                <DataGridTextColumn Header="Última cita" Binding="{Binding UltimaCita, Converter={StaticResource FechaNulaConverter}}" Width="Auto"/>
                <DataGridTextColumn Header="Tipo de cita" Binding="{Binding TipoDeCita}" Width="Auto"/>
                <DataGridTextColumn Header="Próxima cita" Binding="{Binding ProximaCita, Converter={StaticResource FechaNulaConverter}}" Width="Auto"/>
                <DataGridTextColumn Header="Tipo próxima cita" Binding="{Binding TipoProximaCita}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
